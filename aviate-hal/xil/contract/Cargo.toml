[package]
name = "aviate-xil-contract"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Rust-owned shared-memory contract between the Aviate gz-sim plugin, the flight controller, and read-only telemetry consumers"

[dependencies]

[dev-dependencies]
cbindgen = { version = "0.29", default-features = false }

[lints]
workspace = true
