[package]
name = "aviate-signing"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false
description = "MAVLink 2 sha256_48 signing primitive, shared by the boards that verify signatures"

[lints]
workspace = true

[dependencies]
sha2 = { version = "0.10", default-features = false }
