[package]
name = "aviate-core"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
bitflags = "2.4"
libm = "0.2"

[features]
default = ["mc", "fw", "vtol"]
mc = []
fw = []
vtol = []
# Enable test hooks for inject_state/inject_fault (spec §20)
test-hooks = []

[lib]
path = "src/lib.rs"
[lints]
workspace = true
